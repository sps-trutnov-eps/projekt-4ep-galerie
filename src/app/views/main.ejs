<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/global_tagy.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Document</title>
    <style>
        .random
        {
            color: red;
        }
    </style>
</head>
<body>
    <div class="custom_container shadow-block">
        <header>
            <div>
                
            </div>
        </header>
        <main>
            <div>
                <% var tagy=data.tagy %>
                <%= tagy.pocet_projektu %>
                <% for(var i = 0; i < Object.keys(tagy).length; i++){ %>
                <div class="dropdown">
                    <button class="dropbtn"><%= Object.keys(tagy)[i] %></button>
                        <div  class="containerSTlacitky">
                            <% for(let k = 0;k< tagy[Object.keys(tagy)[i]].length;k++) { %>
                                <input class="random" value="<%=tagy[Object.keys(tagy)[i]][k]%>" type="checkbox"><label for=""><%= tagy[Object.keys(tagy)[i]][k]%> %></label><br>
                            <% } %>
                        </div>
                    </div>
                <% } %>
            </div>  
            <div class="row col-12 m-0 mt-5 m-0-a">   
            <% for(let i in data.projekty){ %>  
                <img class="p-0  col-6 shadow-block" src="img/<%= i %>/<%= data.projekty[i].obrazky[0] %>"  height="350px" frameborder="0"/>
            <% } %>     
            </div>
        </main>

        <footer>

        </footer>
    </div>
    <script src="scripts\script_tagy.js"></script>
</body>
</html>